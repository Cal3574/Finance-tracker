import { FC } from "react";

interface StatCardProps {}

const StatCard: FC<StatCardProps> = ({}) => {
  return <div className="w-full h-[8rem] bg-[#2D325A] rounded-xl"></div>;
};

export default StatCard;
